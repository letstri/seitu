---
title: Computed
description: "Derives a subscription from one source or many."
---

## createComputed()
`transform` is memoized on
source references, so `get()` is stable until a source changes.

```ts twoslash
import { createComputed, createStore } from 'seitu'

const count = createStore({ a: 1, b: 2 })
const sum = createComputed(count, s => s.a + s.b)
sum.get() // 3
```
```ts twoslash
import { createComputed, createStore } from 'seitu'

const a = createStore(1)
const b = createStore(2)
const sum = createComputed([a, b], ([a, b]) => a + b)
sum.get()
```
